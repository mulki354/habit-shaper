# Task 1.4: Modul Habits — CRUD

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 4.B, `docs/implementations/20260701_implementation_backend.md`

---

## Instruksi

### Langkah 1: Install Validation Dependencies (jika belum)
- `npm install class-validator class-transformer` (Sudah terpasang di dependencies package.json)

### Langkah 2: HabitsModule Structure
Buat folder `backend/src/habits/` dengan file:
- `habits.module.ts` — import PrismaModule
- `habits.controller.ts` — CRUD endpoints, semua pakai JwtAuthGuard
- `habits.service.ts` — logika bisnis CRUD
- `dto/create-habit.dto.ts` — `{ name: string, type: "BUILD" | "BREAK" }`
- `dto/update-habit.dto.ts` — `{ name: string }` (tipe TIDAK bisa diubah)

### Langkah 3: Endpoint Details

**GET /habits** (Protected)
- Return semua habit milik `req.user.id`
- Untuk sekarang return data mentah, stats ditambahkan di Task 1.5

**POST /habits** (Protected)
- Body: `{ name, type }` — validasi type harus "BUILD" atau "BREAK"
- Set `userId` dari token
- Return: habit baru

**PATCH /habits/:id** (Protected)
- Body: `{ name }` — hanya nama yang bisa diubah
- Validasi: habit harus milik `req.user.id` (404 jika bukan)
- Return: habit terupdate

**DELETE /habits/:id** (Protected)
- Validasi: habit harus milik `req.user.id` (404 jika bukan)
- Cascade delete entries juga (sudah dihandle oleh Prisma onDelete: Cascade)
- Return: 204 No Content

### Langkah 4: Ownership Guard Pattern
- Di setiap endpoint yang butuh `:id`, cari habit dengan `{ id, userId }` — jika null, throw NotFoundException
- Ini memastikan user A tidak bisa akses/ubah/hapus habit user B

### Langkah 5: Register HabitsModule di AppModule

---

## Ceklis

- [x] HabitsModule terbuat dengan semua file
- [x] `GET /habits` — return list habit milik user yang login
- [x] `POST /habits` — buat habit BUILD berhasil
- [x] `POST /habits` — buat habit BREAK berhasil
- [x] `POST /habits` — tipe invalid → 400
- [x] `PATCH /habits/:id` — update nama berhasil
- [x] `PATCH /habits/:id` — habit milik user lain → 404
- [x] `DELETE /habits/:id` — hapus berhasil, return 204
- [x] `DELETE /habits/:id` — habit milik user lain → 404
- [x] Entries ikut terhapus saat habit dihapus (cascade)
- [x] `docker compose up --build` berhasil

---

## Changelog

- Membuat `CreateHabitDto` dan `UpdateHabitDto` untuk memvalidasi payload request habits.
- Membuat `HabitsService` dengan logika pengambilan, pembuatan, pembaruan, dan penghapusan habit yang mengimplementasikan Ownership Guard Pattern.
- Membuat `HabitsController` dengan route `/habits` terlindungi oleh `JwtAuthGuard`.
- Mengintegrasikan `HabitsModule` ke dalam `AppModule`.
- Memperbarui `backend/Dockerfile` agar men-generate Prisma Client pada builder stage guna mencegah kegagalan build TS.

---

## Bukti Verifikasi

```bash
# 1. Registrasi User A
$ curl -X POST http://localhost:4000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user_a@example.com","password":"Password123"}'
{"user":{"id":2,"email":"user_a@example.com"},"accessToken":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjIsImVtYWlsIjoidXNlcl9hQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA1OTg3LCJleHAiOjE3ODM2MTA3ODd9.TtA1dvbdJQ7uG-Z2q6igGJ7d7KaKsEPzF-TqfDjZNwQ"}

# 2. Registrasi User B
$ curl -X POST http://localhost:4000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user_b@example.com","password":"Password123"}'
{"user":{"id":3,"email":"user_b@example.com"},"accessToken":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjMsImVtYWlsIjoidXNlcl9iQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA2MDI1LCJleHAiOjE3ODM2MTA4MjV9.GGiam4wacW1WNlGUSkYRfbwkSLYjqUwYucN4TKzuZ1M"}

# Set token JWT & Habit ID ke variabel bash
$ TOKEN_A="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjIsImVtYWlsIjoidXNlcl9hQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA1OTg3LCJleHAiOjE3ODM2MTA3ODd9.TtA1dvbdJQ7uG-Z2q6igGJ7d7KaKsEPzF-TqfDjZNwQ"
$ TOKEN_B="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjMsImVtYWlsIjoidXNlcl9iQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA2MDI1LCJleHAiOjE3ODM2MTA4MjV9.GGiam4wacW1WNlGUSkYRfbwkSLYjqUwYucN4TKzuZ1M"

# 3. Buat Habit BUILD untuk User A
$ curl -X POST http://localhost:4000/habits \
  -H "Authorization: Bearer $TOKEN_A" \
  -H "Content-Type: application/json" \
  -d '{"name":"Olahraga Pagi","type":"BUILD"}'
{"id":1,"userId":2,"name":"Olahraga Pagi","type":"BUILD","createdAt":"2026-07-02T15:27:31.473Z","updatedAt":"2026-07-02T15:27:31.473Z"}
$ HABIT_ID=1

# 4. Buat Habit BREAK untuk User A
$ curl -X POST http://localhost:4000/habits \
  -H "Authorization: Bearer $TOKEN_A" \
  -H "Content-Type: application/json" \
  -d '{"name":"Doomscrolling","type":"BREAK"}'
{"id":2,"userId":2,"name":"Doomscrolling","type":"BREAK","createdAt":"2026-07-02T15:27:52.199Z","updatedAt":"2026-07-02T15:27:52.199Z"}

# 5. Coba Buat Habit dengan Tipe Invalid (400 Bad Request)
$ curl -X POST http://localhost:4000/habits \
  -H "Authorization: Bearer $TOKEN_A" \
  -H "Content-Type: application/json" \
  -d '{"name":"Habit Aneh","type":"INVALID"}'
{"message":["type must be either BUILD or BREAK"],"error":"Bad Request","statusCode":400}

# 6. Ambil Habit User A (Berisi 2 Habit)
$ curl -X GET http://localhost:4000/habits -H "Authorization: Bearer $TOKEN_A"
[{"id":1,"userId":2,"name":"Olahraga Pagi","type":"BUILD","createdAt":"2026-07-02T15:27:31.473Z","updatedAt":"2026-07-02T15:27:31.473Z"},{"id":2,"userId":2,"name":"Doomscrolling","type":"BREAK","createdAt":"2026-07-02T15:27:52.199Z","updatedAt":"2026-07-02T15:27:52.199Z"}]

# 7. Ambil Habit User B (Harus Kosong [])
$ curl -X GET http://localhost:4000/habits -H "Authorization: Bearer $TOKEN_B"
[]

# 8. Coba Edit Habit User A Menggunakan Token User B (404 Not Found)
$ curl -X PATCH http://localhost:4000/habits/$HABIT_ID \
  -H "Authorization: Bearer $TOKEN_B" \
  -H "Content-Type: application/json" \
  -d '{"name":"Coba Hack"}'
{"message":"Habit dengan ID 1 tidak ditemukan","error":"Not Found","statusCode":404}

# 9. Edit Habit User A Menggunakan Token User A (Berhasil)
$ curl -X PATCH http://localhost:4000/habits/$HABIT_ID \
  -H "Authorization: Bearer $TOKEN_A" \
  -H "Content-Type: application/json" \
  -d '{"name":"Olahraga Pagi Baru"}'
{"id":1,"userId":2,"name":"Olahraga Pagi Baru","type":"BUILD","createdAt":"2026-07-02T15:27:31.473Z","updatedAt":"2026-07-02T15:28:39.047Z"}

# 10. Coba Hapus Habit User A Menggunakan Token User B (404 Not Found)
$ curl -X DELETE http://localhost:4000/habits/$HABIT_ID -H "Authorization: Bearer $TOKEN_B"
{"message":"Habit dengan ID 1 tidak ditemukan","error":"Not Found","statusCode":404}

# 11. Hapus Habit User A Menggunakan Token User A (Berhasil 204)
$ curl -X DELETE http://localhost:4000/habits/$HABIT_ID -H "Authorization: Bearer $TOKEN_A"
(No Output - 204 No Content)
```
