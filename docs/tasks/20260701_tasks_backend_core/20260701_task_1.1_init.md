# Task 1.1: Inisialisasi Proyek + Docker Compose Skeleton

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 2, `docs/implementations/20260701_implementation_backend.md`

---

## Instruksi

### Langkah 1: Inisialisasi NestJS Backend
- Jalankan `npx @nestjs/cli new backend --package-manager npm --strict`
- Pastikan folder `backend/` terbentuk dengan struktur NestJS standar

### Langkah 2: Healthcheck Endpoint
- Tambahkan endpoint `GET /health` di `AppController` yang return `{ status: 'ok' }`

### Langkah 3: Backend Dockerfile
- Buat `backend/Dockerfile` — multi-stage (install deps → build → run)
- Startup command: `node dist/main.js` (nanti di Task 1.2 akan ditambah prisma migrate)

### Langkah 4: Compose.yml
- Buat `compose.yml` di root dengan 3 service:
  - `mysql` — MySQL 8, named volume, healthcheck
  - `backend` — build dari `backend/`, depends_on mysql healthy, port 4000
  - `frontend` — placeholder (image nginx:alpine, port 3000) — akan diganti di Task 2.1

### Langkah 5: Environment Files
- Buat `.env.example` di root sesuai PRD § 2.3
- Buat `.env` (copy dari `.env.example` dengan nilai yang sama — untuk local dev)

---

## Ceklis

- [x] `backend/` folder terbentuk (NestJS TypeScript)
- [x] `GET /health` endpoint berfungsi → `{ status: 'ok' }`
- [x] `backend/Dockerfile` ada dan bisa di-build
- [x] `compose.yml` ada dengan 3 service (mysql, backend, frontend placeholder)
- [x] `.env.example` lengkap sesuai PRD
- [x] `docker compose up --build` berhasil tanpa error
- [x] MySQL healthy, backend accessible di port 4000

---

## Changelog

- `backend/` — NestJS project diinisialisasi dengan `@nestjs/cli`, TypeScript strict mode
- `backend/src/app.controller.ts` — `GET /health` endpoint ditambahkan, return `{ status: 'ok' }`
- `backend/Dockerfile` — multi-stage build (builder: install + build, runner: prod deps only)
- `compose.yml` — 3 service: mysql (port 3306, healthcheck), backend (port 4000, depends_on mysql healthy), frontend placeholder (nginx:alpine, port 3000)
- `.env.example` + `.env` — dibuat sesuai PRD § 2.3 (DATABASE_URL, JWT_SECRET, PORT, VITE_API_URL)

---

## Bukti Verifikasi

### `docker compose ps` — semua container UP
```
NAME                      IMAGE                  SERVICE    STATUS                    PORTS
habit-shaper-mysql-1      mysql:8                mysql      Up (healthy)              0.0.0.0:3306->3306/tcp
habit-shaper-backend-1    habit-shaper-backend   backend    Up                        0.0.0.0:4000->4000/tcp
habit-shaper-frontend-1   nginx:alpine           frontend   Up                        0.0.0.0:3000->80/tcp
```

### `Invoke-RestMethod http://localhost:4000/health` — response OK
```
status
------
ok
```
