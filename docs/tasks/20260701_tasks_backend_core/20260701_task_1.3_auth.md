# Task 1.3: Modul Autentikasi (Register/Login)

**Status:** 💚 Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 4.A, `docs/implementations/20260701_implementation_backend.md`

---

## Instruksi

### Langkah 1: Install Dependencies
- `npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt class-validator class-transformer`
- `npm install -D @types/passport-jwt @types/bcrypt`

### Langkah 2: AuthModule Structure
Buat folder `backend/src/auth/` dengan file:
- `auth.module.ts` — import JwtModule, PassportModule, PrismaModule
- `auth.controller.ts` — 3 endpoint (register, login, me)
- `auth.service.ts` — logika register, login, validasi password
- `dto/register.dto.ts` — `{ email: string, password: string }` dengan class-validator
- `dto/login.dto.ts` — `{ email: string, password: string }`
- `strategies/jwt.strategy.ts` — PassportStrategy(Strategy) untuk extract user dari token
- `guards/jwt-auth.guard.ts` — AuthGuard('jwt')

### Langkah 3: Endpoint Details

**POST /auth/register**
- Validasi email format, password minimal 6 karakter
- Cek email belum terdaftar (409 Conflict jika sudah)
- Hash password dengan bcrypt (saltRounds: 10)
- Buat user di DB
- Generate JWT token
- Return: `{ user: { id, email }, accessToken }`

**POST /auth/login**
- Cari user by email (401 jika tidak ditemukan)
- Compare password dengan hash (401 jika salah)
- Generate JWT token
- Return: `{ accessToken }`

**GET /auth/me** (Protected)
- Pakai JwtAuthGuard
- Return: `{ id, email }` dari req.user

### Langkah 4: JWT Configuration
- Secret dari env `JWT_SECRET`
- ExpiresIn: `'7d'` (cukup untuk testing)
- Payload: `{ sub: user.id, email: user.email }`

### Langkah 5: Register AuthModule di AppModule

---

## Ceklis

- [x] AuthModule terbuat dengan semua file
- [x] `POST /auth/register` — berhasil register user baru, return user + token
- [x] `POST /auth/register` — email duplikat → 409
- [x] `POST /auth/login` — berhasil login, return token
- [x] `POST /auth/login` — email/password salah → 401
- [x] `GET /auth/me` — dengan token valid → return user
- [x] `GET /auth/me` — tanpa token → 401
- [x] Password tersimpan sebagai hash (bukan plaintext)
- [x] `docker compose up --build` berhasil

---

## Changelog

- **Added**: `backend/src/auth/dto/register.dto.ts` (Validasi input register)
- **Added**: `backend/src/auth/dto/login.dto.ts` (Validasi input login)
- **Added**: `backend/src/auth/strategies/jwt.strategy.ts` (Strategi JWT untuk verifikasi token)
- **Added**: `backend/src/auth/guards/jwt-auth.guard.ts` (Guard pelindung endpoint)
- **Added**: `backend/src/auth/auth.service.ts` (Logika utama registrasi, hash bcrypt, login & token generation)
- **Added**: `backend/src/auth/auth.controller.ts` (Endpoint publik & privat autentikasi)
- **Added**: `backend/src/auth/auth.module.ts` (Modul integrasi JWT & Passport)
- **Modified**: `backend/src/app.module.ts` (Registrasi `AuthModule`)
- **Modified**: `backend/src/main.ts` (Pemasangan global `ValidationPipe` & konfigurasi host `'0.0.0.0'`)

---

## Bukti Verifikasi

### 1. Registrasi User Baru (`POST /auth/register`)
```json
{"user":{"id":1,"email":"test@example.com"},"accessToken":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsImVtYWlsIjoidGVzdEBleGFtcGxlLmNvbSIsImlhdCI6MTc4MzAwNDM0MSwiZXhwIjoxNzgzNjA5MTQxfQ.XCK9UmyUydN1OdVcIlG2PgMLolIUG0ZMcWBz6SLZHAc"}
```

### 2. Registrasi Email Duplikat (Conflict 409)
```json
{"message":"Email sudah terdaftar","error":"Conflict","statusCode":409}
```

### 3. Validasi Format Input DTO (Bad Request 400)
```json
{"message":["Format email tidak valid","Password minimal harus 6 karakter"],"error":"Bad Request","statusCode":400}
```

### 4. Login User Sukses (`POST /auth/login`)
```json
{"accessToken":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsImVtYWlsIjoidGVzdEBleGFtcGxlLmNvbSIsImlhdCI6MTc4MzAwNDQxMywiZXhwIjoxNzgzNjA5MjEzfQ.N58KSUaNS9CtDtJDaBUbdC2Gx3gn0-g7sOteY29w5gc"}
```

### 5. Login Password Salah (Unauthorized 401)
```json
{"message":"Email atau password salah","error":"Unauthorized","statusCode":401}
```

### 6. Akses `/auth/me` Tanpa Token (Unauthorized 401)
```json
{"message":"Unauthorized","statusCode":401}
```

### 7. Akses `/auth/me` Dengan Token Valid (HTTP 200 OK)
```json
{"id":1,"email":"test@example.com"}
```
