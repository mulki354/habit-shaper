# Task 1.6: Error Handling, Validation Pipe, CORS

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 2, `docs/implementations/20260701_implementation_backend.md`

---

## Instruksi

### Langkah 1: Global Validation Pipe

Di `backend/src/main.ts`, aktifkan:
```typescript
app.useGlobalPipes(new ValidationPipe({
  whitelist: true,        // strip property yang tidak ada di DTO
  forbidNonWhitelisted: true, // error jika ada property asing
  transform: true,        // auto-transform tipe
}));
```

### Langkah 2: Global Exception Filter (Opsional tapi Direkomendasikan)

Buat `backend/src/common/filters/http-exception.filter.ts`:
- Catch semua HttpException
- Return format JSON konsisten:
```json
{
  "statusCode": 400,
  "message": "Validation failed",
  "errors": ["email must be an email", "password is too short"],
  "timestamp": "2026-07-01T..."
}
```

### Langkah 3: CORS

Di `backend/src/main.ts`:
```typescript
app.enableCors({
  origin: process.env.FRONTEND_URL || 'http://localhost:3000',
  credentials: true,
});
```

Tambahkan `FRONTEND_URL` ke `.env.example` jika belum:
```
FRONTEND_URL=http://localhost:3000
```

Dan update `compose.yml` — pass `FRONTEND_URL` ke backend service.

### Langkah 4: Review & Cleanup
- Pastikan semua DTO sudah pakai decorator class-validator (@IsEmail, @IsString, @IsEnum, @MinLength, @IsOptional, dll)
- Pastikan semua endpoint return format yang konsisten
- Pastikan error message informatif (bukan generic "Internal Server Error")

---

## Ceklis

- [x] Global ValidationPipe aktif — request body invalid → 400 dengan pesan detail
- [x] Property asing di body → 400 (forbidNonWhitelisted)
- [x] Exception filter aktif — format error JSON konsisten
- [x] CORS aktif — request dari frontend origin tidak kena block
- [x] Semua DTO sudah pakai decorator validasi
- [x] `FRONTEND_URL` di `.env.example` dan `compose.yml`
- [x] `docker compose up --build` berhasil

---

## Changelog

- Membuat `HttpExceptionFilter` untuk menangkap seluruh `HttpException` dan merespon dengan format JSON terstandarisasi yang berisi detail pesan kesalahan.
- Mengaktifkan `HttpExceptionFilter` secara global di `main.ts`.
- Memperketat validasi global `ValidationPipe` di `main.ts` dengan opsi `forbidNonWhitelisted: true` agar melarang pengiriman properti asing pada request body.
- Mengaktifkan konfigurasi CORS terintegrasi pada `main.ts` bersumber dari variabel lingkungan `FRONTEND_URL`.
- Menyelaraskan berkas `.env`, `.env.example`, dan `compose.yml` dengan menambahkan variabel `FRONTEND_URL`.

---

## Bukti Verifikasi

```bash
# 1. Login User A
$ curl -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user_a@example.com","password":"Password123"}'
{"accessToken":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjIsImVtYWlsIjoidXNlcl9hQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA4NTM0LCJleHAiOjE3ODM2MTMzMzR9.FK_sC6e0vrETk2ZNT2WT7O--wT5k_aHNQGE5Rl5KQ74"}

$ TOKEN_A="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjIsImVtYWlsIjoidXNlcl9hQGV4YW1wbGUuY29tIiwiaWF0IjoxNzgzMDA4NTM0LCJleHAiOjE3ODM2MTMzMzR9.FK_sC6e0vrETk2ZNT2WT7O--wT5k_aHNQGE5Rl5KQ74"

# 2. Uji Validasi Properti Asing (forbidNonWhitelisted) & Format Exception Filter (Harus Mengembalikan 400 Bad Request)
$ curl -X POST http://localhost:4000/habits \
  -H "Authorization: Bearer $TOKEN_A" \
  -H "Content-Type: application/json" \
  -d '{"name":"Olahraga Sore","type":"BUILD","hackerProperty":"hack"}'
{
  "statusCode": 400,
  "message": "Validation failed",
  "errors": [
    "property hackerProperty should not exist"
  ],
  "timestamp": "2026-07-02T16:09:15.216Z",
  "path": "/habits"
}

# 3. Uji Konfigurasi CORS (Preflight OPTIONS Request)
$ curl -i -X OPTIONS http://localhost:4000/habits \
  -H "Access-Control-Request-Method: GET" \
  -H "Origin: http://localhost:3000"
HTTP/1.1 204 No Content
X-Powered-By: Express
Access-Control-Allow-Origin: http://localhost:3000
Vary: Origin, Access-Control-Request-Headers
Access-Control-Allow-Credentials: true
Access-Control-Allow-Methods: GET,HEAD,PUT,PATCH,POST,DELETE
Content-Length: 0
Date: Thu, 02 Jul 2026 16:09:34 GMT
Connection: keep-alive
Keep-Alive: timeout=5
```
