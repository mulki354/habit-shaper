# Task 1.2: Setup Prisma + Schema + Auto-Migration

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 3, `docs/implementations/20260701_implementation_backend.md`

---

## Instruksi

### Langkah 1: Install Prisma
- `cd backend && npm install prisma @prisma/client`
- `npx prisma init --datasource-provider mysql`
- Ini akan membuat `backend/prisma/schema.prisma` dan update `.env`

### Langkah 2: Tulis Schema
- Edit `backend/prisma/schema.prisma` sesuai PRD § 3:
  - Model `User` (id, email unique, passwordHash, createdAt, habits[])
  - Enum `HabitType` (BUILD, BREAK)
  - Model `Habit` (id, userId, name, type, createdAt, updatedAt, entries[])
  - Enum `EntryKind` (COMPLETED, RELAPSED)
  - Model `HabitEntry` (id, habitId, date @db.Date, kind, createdAt, @@unique([habitId, date]))

### Langkah 3: Buat Migration Awal
- `npx prisma migrate dev --name init`

### Langkah 4: PrismaModule (NestJS)
- Buat `backend/src/prisma/prisma.module.ts` dan `backend/src/prisma/prisma.service.ts`
- PrismaService extends PrismaClient, implements OnModuleInit
- Export PrismaModule sebagai global module

### Langkah 5: Auto-Migration di Container Startup
- Update `backend/Dockerfile` — copy folder `prisma/` ke image
- Update startup command: `npx prisma migrate deploy && node dist/main.js`
- Bisa pakai shell script `start.sh` atau langsung di CMD

### Langkah 6: Update compose.yml (jika perlu)
- Pastikan `DATABASE_URL` env var sudah di-pass ke backend service

---

## Ceklis

- [x] Prisma terinstall di backend (versi 6.19.3)
- [x] `schema.prisma` sesuai PRD § 3 (User, Habit, HabitEntry, enum)
- [x] Migration awal berhasil dibuat
- [x] PrismaModule & PrismaService ada dan terdaftar di AppModule
- [x] Dockerfile updated — copy prisma/, auto migrate deploy
- [x] `docker compose up --build` dari database kosong → tabel otomatis terbentuk
- [x] Verifikasi: `docker compose exec mysql mysql -u... -e "SHOW TABLES;"`

---

## Changelog

- Menginstal Prisma CLI dan `@prisma/client` versi 6.19.3 di backend.
- Menulis skema database di `prisma/schema.prisma` yang mencakup model `User`, `Habit`, `HabitEntry`, serta enum `HabitType` dan `EntryKind`.
- Menjalankan migrasi pertama `20260702130629_init` untuk menyinkronkan skema ke MySQL local.
- Membuat `PrismaService` dan `PrismaModule` global di NestJS untuk kemudahan injeksi ketergantungan.
- Mengintegrasikan file `backend/.dockerignore` untuk mempercepat proses build Docker.
- Memperbarui `tsconfig.build.json` dengan mengecualikan `prisma.config.ts` agar build output rapi di `dist/main.js`.
- Memperbarui `Dockerfile` backend untuk menyalin folder `prisma/`, melakukan `npx prisma generate` saat build, dan menjalankan `npx prisma migrate deploy` secara otomatis saat booting kontainer.

---

## Bukti Verifikasi

### Uji Kesehatan API (`curl http://localhost:4000/health`)
```json
{"status":"ok"}
```

### Tabel Database Terbentuk Otomatis (`docker compose exec mysql mysql -u habit_shaper -pchangeme habit_shaper -e "SHOW TABLES;"`)
```
+------------------------+
| Tables_in_habit_shaper |
+------------------------+
| Habit                  |
| HabitEntry             |
| User                   |
| _prisma_migrations     |
+------------------------+
```
