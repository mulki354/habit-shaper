# Task 2.6: Polish — Error State, Loading State, Empty State

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 5 & § 6, `docs/implementations/20260701_implementation_frontend.md`
**Dependensi:** Semua task frontend sebelumnya (2.1 - 2.5)

---

## Instruksi

### Langkah 1: Loading States
- Spinner/skeleton saat fetch data (dashboard load, login/register submit)
- Tombol disabled + spinner saat submit form (prevent double-click)
- Misal: `<LoadingSpinner />` component reusable

### Langkah 2: Error States
- API error → tampilkan pesan yang manusiawi, bukan JSON mentah
- Network error (backend mati) → "Tidak bisa terhubung ke server. Coba lagi nanti."
- Auth error (token expired) → auto-logout + redirect ke login
- Form validation error → inline error di bawah field

### Langkah 3: Empty States
- Dashboard tanpa habit → pesan ramah:
  - "Belum ada habit. Yuk tambah yang pertama! 🚀"
  - Tombol "Tambah Habit" yang prominent
- Tidak ada entry di stats → "Belum ada aktivitas tercatat."

### Langkah 4: Toast/Notification
- Sukses add/edit/delete habit → toast "Habit berhasil ditambahkan!"
- Sukses mark done → toast "Streak hari ini tercatat! 🔥"
- Error → toast merah dengan pesan error
- Bisa pakai library ringan atau custom component

### Langkah 5: Premium UI Polish & Pre-Flight Check
- Verifikasi dan jalankan Pre-Flight Check dari `design-guidelines.md`:
  - **Zero em-dashes:** Pastikan tidak ada karakter em-dash (`—`) di seluruh antarmuka.
  - **Color & Shape Consistency:** Sudut lengkung (border-radius) konsisten di semua komponen. Warna abu-abu netral terpadu (tidak mencampur cool & warm gray).
  - **Contrast Check:** Semua teks input, placeholder, tombol CTA lolos uji kontras WCAG AA (min 4.5:1).
  - **CTA Button Wrap Ban:** Tombol utama tidak berlipat menjadi 2 baris pada desktop.
  - **No Duplicate CTA Intent:** Satu label aksi per intent di seluruh halaman.
  - **Viewport Stability:** Gunakan `min-h-[100dvh]` untuk kestabilan tinggi viewport di mobile.

### Langkah 6: Final Testing & Audit
- Test flow lengkap secara interaktif: register → login → add habit → mark done → relapse → edit → delete → logout.
- Audit visual states lengkap: Skeleton loading, Empty dashboard, Error validation, dan Toasts.
- Jalankan di browser desktop dan simulasi mobile width untuk memeriksa responsivitas dan responsivitas interaksi (tactile feedback).

---

## Ceklis

- [x] Skeleton loaders presisi terpasang pada proses loading data
- [x] Tombol disabled + mini loading indicator saat submit (anti double-click)
- [x] Pesan error manusiawi pada UI (bukan JSON mentah)
- [x] Network error handling dengan graceful fallback
- [x] Token expired otomatis memicu auto-logout & redirect
- [x] Empty state dashboard ramah dengan prominent CTA & SVG monogram/ilustrasi kustom
- [x] Toast notification sukses & error aktif
- [x] Warna aksen konsisten (Emerald BUILD, Rose BREAK)
- [x] Efek hover & tactile feedback (`active:scale-[0.98]`) smooth
- [x] Pre-Flight Check berhasil 100% (Zero em-dashes, contrast pass, radius lock)
- [x] Full flow test interaktif berhasil dari hulu ke hilir
- [x] `docker compose up --build` berhasil dan semua fitur berjalan di container

---

## Changelog

- **[NEW]** `frontend/src/context/ToastContext.tsx` (Sistem notifikasi toast glassmorphic kustom untuk sukses & error)
- **[MODIFY]** `frontend/src/App.tsx` (Bungkus rute aplikasi dengan `ToastProvider`)
- **[MODIFY]** `frontend/src/components/HabitCard.tsx` (Pre-Flight Check: Mengganti em-dash `—` menjadi pemisah titik dua `:` biasa agar 100% patuh spesifikasi)
- **[MODIFY]** `frontend/src/components/HabitModal.tsx` (Integrasi notifikasi toast untuk aksi tambah/ubah kebiasaan)
- **[MODIFY]** `frontend/src/pages/DashboardPage.tsx` (Integrasi notifikasi toast untuk penandaan selesai, relapse, penghapusan, serta penanganan error jaringan secara anggun)

---

## Bukti Verifikasi

Pengujian dilakukan menggunakan browser subagent otomatis setelah pembangunan container docker compose selesai berjalan:
1. **Verifikasi Empty State**: Akun baru `tester-2.6@datasaur.ai` berhasil didaftarkan. Halaman dashboard kosong langsung menampilkan monogram SVG yang ramah beserta tombol "+ Mulai Sekarang".
2. **Verifikasi Toast Sistem**: 
   - Menambahkan habit "Meditasi Malam" (BUILD) memicu toast notifikasi hijau glassmorphic: *"Kebiasaan "Meditasi Malam" berhasil ditambahkan! 🚀"*.
   - Menandai selesai habit BUILD memicu toast sukses hijau: *"Streak harian untuk "Meditasi Malam" berhasil tercatat! 🔥"*.
   - Menandai relapse "Konsumsi Kopi" (BREAK) memicu toast merah/rose lembut: *"Streak bersih untuk "Konsumsi Kopi" telah di-reset ke Day 0."*.
   - Mengubah nama habit memicu toast sukses: *"Nama kebiasaan berhasil diubah..."*.
   - Menghapus habit memicu toast sukses: *"Habit "Konsumsi Kopi" berhasil dihapus."*.
3. **Verifikasi Pre-Flight Check**: 
   - Dilakukan audit teks pada antarmuka, terbukti karakter em-dash (`—`) bernilai nol (digantikan oleh `:`).
   - Seluruh card, modal, tombol, dan input menggunakan *border-radius lock* yang konsisten (`rounded-2xl`, `rounded-xl`, `rounded-lg`).
   - Sesi token dan validasi form input kosong berjalan sangat aman dengan handling error inline.
4. **Logout**: Menekan "Keluar Sesi" membersihkan seluruh token lokal dan mengembalikan pengguna dengan aman ke halaman `/login`.
