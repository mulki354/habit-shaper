# Task 2.6: Polish — Error State, Loading State, Empty State

**Status:** ⬜ Belum mulai
**Mengacu:** `docs/PRD_HabitShaper.md` § 5 & § 6, `docs/implementations/20260701_implementation_frontend.md`
**Dependensi:** Semua task frontend sebelumnya (2.1 - 2.5)

---

## Instruksi

### Langkah 1: Loading States
- Spinner/skeleton saat fetch data (dashboard load, login/register submit)
- Tombol disabled + spinner saat submit form (prevent double-click)
- Misal: `<LoadingSpinner />` component reusable

### Langkah 2: Error States
- API error → tampilkan pesan yang manusiawi, bukan JSON mentah
- Network error (backend mati) → "Tidak bisa terhubung ke server. Coba lagi nanti."
- Auth error (token expired) → auto-logout + redirect ke login
- Form validation error → inline error di bawah field

### Langkah 3: Empty States
- Dashboard tanpa habit → pesan ramah:
  - "Belum ada habit. Yuk tambah yang pertama! 🚀"
  - Tombol "Tambah Habit" yang prominent
- Tidak ada entry di stats → "Belum ada aktivitas tercatat."

### Langkah 4: Toast/Notification
- Sukses add/edit/delete habit → toast "Habit berhasil ditambahkan!"
- Sukses mark done → toast "Streak hari ini tercatat! 🔥"
- Error → toast merah dengan pesan error
- Bisa pakai library ringan atau custom component

### Langkah 5: Final UI Polish
- Pastikan warna konsisten (emerald untuk BUILD, rose untuk BREAK)
- Spacing, typography, alignment rapi
- Hover states pada tombol dan card
- Transition/animasi halus pada modal open/close

### Langkah 6: Final Testing
- Test flow lengkap: register → login → add habit → mark done → relapse → edit → delete → logout
- Test edge case: backend mati, token expired, double-click, form kosong
- Test di browser desktop & mobile width

---

## Ceklis

- [ ] Loading spinner saat fetch data
- [ ] Button disabled + spinner saat submit
- [ ] Error message manusiawi (bukan JSON mentah)
- [ ] Network error handling — pesan "tidak bisa terhubung"
- [ ] Token expired → auto-logout
- [ ] Empty state dashboard — pesan ramah + CTA
- [ ] Toast notification sukses & error
- [ ] Warna konsisten (emerald BUILD, rose BREAK)
- [ ] Hover states dan transisi smooth
- [ ] Full flow test berhasil: register → login → CRUD → tracking → logout
- [ ] `docker compose up --build` berhasil — semua fitur berfungsi

---

## Changelog

_(diisi setelah selesai)_

---

## Bukti Verifikasi

_(paste screenshot loading, error, empty state, dan full flow test di sini)_
