# Task 2.2: Routing + Halaman Login/Register

**Status:** ✅ Selesai

**Mengacu:** `docs/PRD_HabitShaper.md` § 4.A & § 5, `docs/implementations/20260701_implementation_frontend.md`
**Dependensi:** Task 1.3 (Auth backend) harus sudah selesai

---

## Instruksi

### Langkah 1: Install React Router
- `cd frontend && npm install react-router-dom`

### Langkah 2: Setup Router
Di `App.tsx`, setup routes:
```
/login     → LoginPage
/register  → RegisterPage
/dashboard → DashboardPage (protected)
/          → redirect ke /dashboard jika login, /login jika tidak
```

### Langkah 3: Auth Context/State
Buat `frontend/src/lib/auth.tsx` (atau context):
- State: `accessToken`, `user` (id, email)
- `login(token)` — simpan token di state + localStorage
- `logout()` — hapus token, redirect ke /login
- `isAuthenticated` — cek apakah token ada
- Load token dari localStorage saat app init (survive refresh)

### Langkah 4: Protected Route Component
Buat `frontend/src/components/ProtectedRoute.tsx`:
- Jika `isAuthenticated` → render children
- Jika tidak → redirect ke `/login`

### Langkah 5: Halaman Register
`frontend/src/pages/RegisterPage.tsx`:
- Form: email, password, confirm password
- Submit → `POST /auth/register`
- Sukses → simpan token, redirect ke `/dashboard`
- Error → tampilkan pesan (email sudah terdaftar, dll)

### Langkah 6: Halaman Login
`frontend/src/pages/LoginPage.tsx`:
- Form: email, password
- Submit → `POST /auth/login`
- Sukses → simpan token, redirect ke `/dashboard`
- Error → tampilkan pesan (email/password salah)

### Langkah 7: Link antar halaman
- Di Login: "Belum punya akun? Register"
- Di Register: "Sudah punya akun? Login"

---

## Ceklis

- [x] React Router terinstall dan routing berfungsi
- [x] Auth context/state terbuat — token persist di localStorage
- [x] ProtectedRoute component berfungsi — redirect ke login jika belum auth
- [x] Register page — form valid, submit ke API, handle sukses & error
- [x] Login page — form valid, submit ke API, handle sukses & error
- [x] Setelah register/login → redirect ke dashboard
- [x] Refresh halaman → sesi tetap ada (token dari localStorage)
- [x] Logout → token dihapus, redirect ke login
- [x] `docker compose up --build` berhasil

---

## Changelog

- Menginstal paket `react-router-dom` di frontend.
- Membuat React Context `AuthContent.tsx` untuk mengelola data user, token JWT, persistensi sesi via `localStorage`, dan fetch profil user otomatis di startup.
- Membuat komponen rute terproteksi `ProtectedRoute.tsx` lengkap dengan skeleton loading visual.
- Membuat halaman formulir masuk `LoginPage.tsx` dan pendaftaran `RegisterPage.tsx` sesuai aturan anti-slop (label di atas, validation inline error, warna aksen Emerald/Zinc, tactile scale transition).
- Membuat halaman `DashboardPage.tsx` sementara sebagai halaman redirect yang aman lengkap dengan tombol logout.
- Mengintegrasikan navigasi router utama di `App.tsx`.

---

## Bukti Verifikasi

Pengujian dilakukan menggunakan browser subagent dan berjalan sukses:
1. **Pengalihan Otomatis**: Membuka `/` otomatis dialihkan ke `/login`.
2. **Registrasi**: Mengisi email `testuser@example.com` dan kata sandi di `/register` sukses, user otomatis masuk dan dialihkan ke `/dashboard` dengan data email tertera di header.
3. **Persistensi Sesi**: Melakukan refresh pada `/dashboard` tetap menahan user pada halaman tersebut.
4. **Logout**: Mengeklik "Keluar Sesi" sukses menghapus token dari `localStorage` dan mengembalikan user ke halaman `/login`.
5. **Guard Rute**: Mengakses langsung URL `/dashboard` dalam keadaan belum login otomatis diblokir dan dialihkan kembali ke `/login`.

