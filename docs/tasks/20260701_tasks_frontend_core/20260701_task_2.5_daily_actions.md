# Task 2.5: Aksi Harian — Mark Done / Mark Relapse

**Status:** ✅ Selesai
**Mengacu:** `docs/PRD_HabitShaper.md` § 1.1.A & § 1.1.B & § 4.B, `docs/implementations/20260701_implementation_frontend.md`
**Dependensi:** Task 1.5 (Tracking backend), Task 2.3 (Dashboard)

---

## Instruksi

### Langkah 1: Tombol Mark Done (BUILD)
Di setiap HabitCard bertipe BUILD:
- Tombol "✅ Mark Done Today"
- Klik → `POST /habits/:id/complete` (tanpa body, default hari ini)
- Sukses → refresh stats, streak bertambah
- **Disabled** jika hari ini sudah ditandai (cek dari data entries/stats)
- Visual feedback: tombol berubah jadi "✅ Done!" (disabled, warna hijau)

### Langkah 2: Tombol Mark Relapse (BREAK)
Di setiap HabitCard bertipe BREAK:
- Tombol "⚠️ I Relapsed"
- Klik → **konfirmasi dulu**: "Tandai relapse hari ini? Streak akan reset ke Day 0."
- Confirm → `POST /habits/:id/relapse`
- Sukses → refresh stats, streak reset ke 0
- **Disabled** jika hari ini sudah ditandai relapse

### Langkah 3: Visual Feedback Setelah Aksi
- BUILD setelah mark done:
  - Streak number animasi naik (opsional, minimal angka berubah)
  - Weekly rate ter-update
  - Tombol disabled sampai besok

- BREAK setelah relapse:
  - Streak reset ke 0 — tampilkan state yang jelas (bukan cuma angka berubah diam-diam)
  - Misal: badge "Reset — Day 0" dengan warna merah
  - Tombol disabled untuk hari ini

### Langkah 4: Deteksi "Sudah Ditandai Hari Ini"
- Menggunakan properti `isActionDoneToday` yang dikirim dari backend dalam objek habit hasil `GET /habits` (pendekatan efisien untuk menghindari N+1 query).
- Jika `isActionDoneToday` bernilai `true` -> tombol disabled + label berubah ("✓ Selesai Hari Ini" atau "⚠️ Relapse Hari Ini").
- Jika terjadi attempt duplikat, handle response 409 (Conflict) dari backend.

---

## Ceklis

- [x] Tombol "Mark Done Today" di card BUILD — klik → streak berubah
- [x] Tombol disabled jika hari ini sudah ditandai
- [x] Tombol "I Relapsed" di card BREAK — klik → konfirmasi → streak reset
- [x] Konfirmasi sebelum relapse (karena destruktif)
- [x] Visual feedback jelas setelah aksi (bukan hanya angka berubah diam-diam)
- [x] Weekly completion rate ter-update setelah mark done
- [x] Handle error: 409 duplicate, 400 tipe salah
- [x] `docker compose up --build` berhasil

---

## Changelog

- **[MODIFY]** `backend/src/habits/habits.service.ts` (Menambahkan `isActionDoneToday` di response stats `GET /habits` untuk tipe BUILD & BREAK)
- **[MODIFY]** `frontend/src/components/HabitCard.tsx` (Update interface `Habit` dan logikanya untuk menonaktifkan tombol aksi jika sudah ditandai hari ini)
- **[NEW]** `frontend/src/components/ConfirmRelapseModal.tsx` (Modal konfirmasi kustom sebelum pengguna menandai relapse)
- **[MODIFY]** `frontend/src/pages/DashboardPage.tsx` (Koneksi aksi harian ke endpoint backend `/complete` dan `/relapse`, serta integrasi modal relapse)

---

## Bukti Verifikasi

Pengujian dilakukan menggunakan browser subagent otomatis setelah pembangunan container docker compose selesai berjalan:
1. **Registrasi Akun Baru**: Sesi lama berhasil dikeluarkan dan akun baru dengan email `tester-2.5@datasaur.ai` berhasil terdaftar, langsung diarahkan ke Dashboard.
2. **Aksi Kebiasaan Positif (BUILD)**: Membuat kebiasaan "Olahraga Pagi". Mengklik tombol "Tandai Selesai Hari Ini" berhasil mengirimkan data ke backend. Statistik kepatuhan mingguan berubah menjadi "1/7 hari", streak bertambah menjadi "1 hari", dan tombol berubah status menjadi dinonaktifkan (disabled) dengan teks "✓ Selesai Hari Ini".
3. **Aksi Kebiasaan Negatif (BREAK - Relapse)**: Membuat kebiasaan "Doomscrolling". Mengklik "Saya Relapse Hari Ini" memicu modal konfirmasi kustom yang didesain secara gelap (dark mode) dengan background blur. Memilih "Ya, Relapse" berhasil me-reset streak bersih menjadi "0 hari", memunculkan badge merah penegas "Relapse Hari Ini — Streak Reset", serta mengubah tombol menjadi dinonaktifkan dengan tulisan "⚠️ Relapse Hari Ini".
4. **Pengujian Persistensi**: Halaman dashboard di-refresh berulang kali; status penonaktifan tombol harian beserta nilai streak terbaru tetap bertahan dan tersimpan dengan baik di database.

